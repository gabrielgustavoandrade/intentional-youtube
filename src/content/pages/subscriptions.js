// Subscriptions page cleanup

IntentionalYT.pages = IntentionalYT.pages || {};

IntentionalYT.pages.subscriptions = (() => {
  function clean() {
    // Shorts and suggestedMixes already handled by globalCleanup
  }

  return { clean };
})();
